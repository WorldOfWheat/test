#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int n, m;
vector<int> arr;

void solve()
{
    cin >> n >> m;
    
    arr.resize(n);
    arr.push_back()
    
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    solve();

    return 0;
}