#include <bits/stdc++.h>
#include <ranges> 

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

void solve()
{
    int a = (int) 'a';
    cout << a << '\n';
    cout << oct << a << '\n';
    cout << hex << a;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    solve();

    return 0;
}