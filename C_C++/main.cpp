#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int n, m;
vector<int> ve;

void solve() 
{
	cout << setprecision(2) << 1.0 / 1.0;

}

int main() 
{
	ios::sync_with_stdio(false);
	cin.tie(0);
    cout.tie(0);

	solve();

	return 0;
}