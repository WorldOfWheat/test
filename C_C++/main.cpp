#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

void solve() 
{
	for (int i = 0; i < 5; i++)
	{
		for (int j = 0; j < 5; j++)
		{
			cout << "Eason is dian" << ' ';
		}
		cout << endl;
	}
}

int main() 
{
	ios::sync_with_stdio(false);
	cin.tie(0);
    cout.tie(0);

	solve();

	return 0;
}