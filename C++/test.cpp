#include <bits/stdc++.h>

using namespace std;

void solve() 
{
    int a;
    cin >> a;
    if (a == 5)
    {
        goto skip;
    }

    cout << "test" << '\n';

    skip:
    return;
}

signed main() 
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}