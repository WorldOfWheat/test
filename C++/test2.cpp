#include <bits/stdc++.h>
// #define FastIO

using namespace std;

signed main() 
{
    #ifdef FastIO
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    #endif

    cout << ""

    return 0;
}